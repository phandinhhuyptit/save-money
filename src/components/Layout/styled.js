import styled from 'styled-components';
export const Wrapper = styled.div`
  .header-wrapper {
    .menu-wrapper {
      .list {
        list-style: none;
        display: flex;
        justify-content: space-between;
      }
    }
  }
`;
